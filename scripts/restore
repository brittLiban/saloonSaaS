#!/usr/bin/env sh
set -eu

if [ "${1:-}" = "" ]; then
  echo "Usage: scripts/restore path/to/postgres.sql" >&2
  exit 1
fi

docker compose exec -T postgres psql \
  -U "${POSTGRES_USER:-glasshound}" \
  -d "${POSTGRES_DB:-glasshound}" \
  < "$1"

echo "Restore complete from $1"
