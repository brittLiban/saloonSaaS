# Prototypes

This folder holds source design/prototype exports that are not part of the production app build.

- `claude-design-export/` contains the original static HTML/CSS/JSX export.

Use these files as visual and product-reference material while implementing the production app under `src/`.
